services:
  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile.local
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      app:
        condition: service_started

  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "8000"
